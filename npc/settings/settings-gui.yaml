# These are the default gui settings for NPC.
#
# DO NOT EDIT THIS FILE.
#
# Instead, put your custom settings in "~/.config/npc/settings-gui.yaml".
# The command `npc settings campaign -t gui` will open it for you.
# Settings can also be put in campaign-specific setting files, for example, in
# "My Campaign/.npc/settings-gui.yaml".
---
gui:
  defaults:

    # The default character type to use for the new character dialog
    character_type: human
