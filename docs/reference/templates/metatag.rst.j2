@{{ metatag.name }}
{{ header_characters[header_level] * (metatag.name | length + 1) }}

{{ metatag.desc }}

{{ metatag.doc | replace("\n", "\n\n") }}

{%- if metatag.static %}
Static Tags:

{% for static_key, static_value in metatag.static | items -%}
- @{{ static_key }}: ``{{ static_value }}``
{% endfor -%}
{%- endif -%}

{%- if metatag.match %}
Matches Tags:

{% for match in metatag.match -%}
- @{{ match }}
{% endfor %}
separator: "{{ metatag.separator }}"

greedy: {% if metatag.greedy %}yes{% else %}no{% endif %}
{%- endif -%}
